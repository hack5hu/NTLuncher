import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  rename: {
    width: '60%',
    padding: 10,
    borderWidth: 1,
    borderColor: 'gray',
    borderRadius: 5,
    marginBottom: 10,
    backgroundColor: 'red',
  },
});
